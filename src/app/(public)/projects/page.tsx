import { FoundationPage, foundationPageMetadata } from "@/components/foundation-page";

export const metadata = foundationPageMetadata("projects");

export default function ProjectsPage() {
  return <FoundationPage section="projects" />;
}
